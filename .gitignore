node_modules/
dist/
build/
.env
.env.local
*.log
coverage/

# The local document store (STORAGE_PATH), anchored to the repository root.
# Unanchored, this pattern also matched apps/api/src/services/storage/ and
# silently excluded that module's source from every commit — the files existed
# locally, so everything passed here and only CI saw the missing module.
/storage/

.DS_Store
*.tsbuildinfo
.vite/
